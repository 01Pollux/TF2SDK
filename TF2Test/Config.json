{
  "info": {
    "paused": false, //	if true, plugin will start up paused
    "interface": false, //	if true, plugin is sharing interface(s)
    "process": [ //	the plugin will only be loaded into those processes
      "hl2.exe"
    ],
    "deps": [ //	the plugin will requires this list of plugins in order to work propertly
      //"ImGui interface"
    ],
    "auto load": false, //	start up by calling IPlugin::OnLoadConfig during load
    "auto save": false //	end up calling IPlugin::OnSaveConfig during unload
  },
  "vars": {
  }
}